package SinglyLinkedList;

class LinkedList {
    Node head; // head of list

    /*
        LinkedList Node
    */
    class Node {
        Integer data;
        Node next;
        Node(Integer data) { this.data = data; }
    }

}